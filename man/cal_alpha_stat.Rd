% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cal_alpha_stat.R
\name{cal_alpha_stat}
\alias{cal_alpha_stat}
\title{Compute Pairwise Statistical Tests for Alpha Diversity Metrics}
\usage{
cal_alpha_stat(
  alpha,
  metadata,
  group_col,
  metrics = c("Observed", "Shannon", "Chao1", "Simpson"),
  method = "wilcox",
  p.adjust.method = "BH",
  strata = NULL
)
}
\arguments{
\item{alpha}{A \code{data.frame} containing alpha diversity metrics (e.g., output of \code{alpha_cal()}).}

\item{metadata}{A \code{data.frame} with sample-level metadata, including grouping and optional stratification variables.}

\item{group_col}{A string specifying the column in \code{metadata} used for group comparison (e.g., "treatment").}

\item{metrics}{A character vector of alpha metrics to compare (default: \code{c("Observed", "Shannon", "Chao1", "Simpson")}).}

\item{method}{Statistical test to use: \code{"wilcox"} (default) or \code{"t.test"}.}

\item{p.adjust.method}{Method for p-value adjustment (default: \code{"BH"} for Benjamini-Hochberg).}

\item{strata}{Optional. A string specifying a column in \code{metadata} used for stratification (e.g., "timeline").}
}
\value{
A named list where each element is a tibble of pairwise test results for one alpha metric.
}
\description{
This function calculates pairwise statistical comparisons (e.g., Wilcoxon or t-test)
for specified alpha diversity metrics across groups defined in sample metadata.
It supports optional stratification by a second variable (e.g., timeline).
}
\details{
The function automatically detects and matches sample identifier columns between \code{alpha} and \code{metadata}.
It performs pairwise statistical comparisons for each alpha diversity metric using \code{rstatix::pairwise_wilcox_test()}
or \code{rstatix::pairwise_t_test()} and optionally stratifies the comparisons by a second grouping variable.
}
\seealso{
\code{\link{plot_alpha}}, \code{\link[rstatix]{pairwise_wilcox_test}}, \code{\link[rstatix]{pairwise_t_test}}
}
