% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_alpha.R
\name{plot_alpha}
\alias{plot_alpha}
\title{Plot Alpha Diversity Boxplots and Export Plots to Global Environment}
\usage{
plot_alpha(
  alpha,
  metadata,
  metrics = c("Observed", "Shannon", "Chao1", "Simpson"),
  x,
  facet = NULL
)
}
\arguments{
\item{alpha}{A \code{data.frame} containing alpha diversity metrics (e.g., from \code{alpha_cal()}).}

\item{metadata}{A \code{data.frame} containing sample metadata (must share an ID column with \code{alpha}).}

\item{metrics}{A character vector of metrics to visualize (e.g., c("Observed", "Shannon")).}

\item{x}{A string specifying the column in metadata to use for the x-axis (e.g., "treatment").}

\item{facet}{Optional. A string specifying the metadata column to facet by (e.g., "timeline").}
}
\value{
A named list with:
\describe{
\item{plots}{A named list of individual \code{ggplot} objects, keyed by metric name.}
\item{combined}{A combined \code{patchwork} plot of all provided metrics.}
}
}
\description{
This function generates boxplots for specified alpha diversity metrics using \code{ggplot2}
and optionally facets them by a grouping variable. It also assigns each individual plot
and the combined patchwork plot to the global environment for direct access.
}
\details{
This function automatically attempts to match the sample identifier columns between
\code{alpha} and \code{metadata} by comparing overlapping values. It uses \code{patchwork} to combine plots.
The result includes individual plots assigned to global environment (e.g., \code{Observed}, \code{Shannon})
and a combined object named \code{alpha_combined}.
}
\seealso{
\code{\link{alpha_cal}}, \code{\link[ggplot2]{ggplot}}, \code{\link[patchwork]{wrap_plots}}
}
