% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DEseq2_cross.R
\name{DEseq2_cross}
\alias{DEseq2_cross}
\title{Differential Abundance Analysis (Cross-sectional) using DESeq2}
\usage{
DEseq2_cross(physeq, group = NULL, comparison = NULL, padj_cutoff = 0.05)
}
\arguments{
\item{physeq}{A \code{phyloseq} object containing taxonomic count data, taxonomy, and sample metadata.}

\item{group}{Character. The name of the grouping variable in \code{sample_data(physeq)} (e.g., "When"). Default is NULL.}

\item{comparison}{Character vector of length 2. The two group values to compare (e.g., \code{c("Early", "Late")}). Default is NULL.}

\item{padj_cutoff}{Numeric. Adjusted p-value threshold to filter significant results (default = 0.05).}
}
\value{
A list with two elements:
\describe{
\item{sig_table}{A data frame of significantly differentially abundant genera.}
\item{plot}{A ggplot2 bar plot showing log2 fold changes of significant genera.}
}
}
\description{
This function performs differential abundance analysis using DESeq2
for a cross-sectional study design where there is only one grouping variable
(e.g., "Early" vs "Late"), without repeated measures or time dependency.
}
