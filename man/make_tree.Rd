% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_tree.R
\name{make_tree}
\alias{make_tree}
\title{make_tree: Build and optionally plot a phylogenetic tree from ASV sequences in a phyloseq object}
\usage{
make_tree(
  ps,
  msa_method = "ClustalW",
  optimize_ml = TRUE,
  model = "GTR",
  optimize_gamma = TRUE,
  return_tree_only = FALSE,
  plot_tree = TRUE
)
}
\arguments{
\item{ps}{A phyloseq object containing ASV sequences in the refseq slot (DNAStringSet)}

\item{msa_method}{Alignment method to use: "ClustalW", "Muscle", or "ClustalOmega"}

\item{optimize_ml}{Logical; whether to optimize the tree using a maximum likelihood model (default: TRUE)}

\item{model}{Substitution model used for ML tree optimization (default: "GTR")}

\item{optimize_gamma}{Logical; whether to optimize the gamma rate parameter (default: TRUE)}

\item{return_tree_only}{Logical; if TRUE, return only the tree (phylo object). If FALSE, return a phyloseq object with the tree attached.}

\item{plot_tree}{Logical; whether to plot the resulting tree using ggtree (default: TRUE)}
}
\value{
A \code{phylo} object (tree only) or an updated \code{phyloseq} object with the phylogenetic tree attached
}
\description{
make_tree: Build and optionally plot a phylogenetic tree from ASV sequences in a phyloseq object
}
\examples{
ps <- make_tree(ps, msa_method = "ClustalOmega", optimize_ml = TRUE)
tree <- make_tree(ps, optimize_ml = FALSE, return_tree_only = TRUE)
}
